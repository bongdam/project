diff --git a/src/libcharon/plugins/kernel_netlink/kernel_netlink_shared.c b/src/libcharon/plugins/kernel_netlink/kernel_netlink_shared.c
index f7ce992..467f6db 100644
--- a/src/libcharon/plugins/kernel_netlink/kernel_netlink_shared.c
+++ b/src/libcharon/plugins/kernel_netlink/kernel_netlink_shared.c
@@ -188,7 +188,7 @@ static ssize_t read_msg(private_netlink_socket_t *this,
 	len = recv(this->socket, buf, buflen, MSG_TRUNC|(block ? 0 : MSG_DONTWAIT));
 	if (len > buflen)
 	{
-		DBG1(DBG_KNL, "netlink response exceeds buffer size");
+		DBG1(DBG_KNL, "netlink response exceeds buffer size len=%zd buflen=%zu", len, buflen);
 		return 0;
 	}
 	if (len < 0)
@@ -590,7 +590,7 @@ netlink_socket_t *netlink_socket_create(int protocol, enum_name_t *names,
 			pagesize = 4096;
 		}
 		/* base this on NLMSG_GOODSIZE */
-		this->buflen = min(pagesize, 8192);
+		this->buflen = 16000;
 	}
 	if (this->socket == -1)
 	{
diff --git a/src/libcharon/sa/ike_sa.c b/src/libcharon/sa/ike_sa.c
index bcbff32..000076c 100644
--- a/src/libcharon/sa/ike_sa.c
+++ b/src/libcharon/sa/ike_sa.c
@@ -651,7 +651,7 @@ METHOD(ike_sa_t, send_keepalive, void,
 		data.ptr[0] = 0xFF;
 		data.len = 1;
 		packet->set_data(packet, data);
-		DBG1(DBG_IKE, "sending keep alive to %#H", this->other_host);
+		DBG2(DBG_IKE, "sending keep alive to %#H", this->other_host);
 		charon->sender->send_no_marker(charon->sender, packet);
 		diff = 0;
 	}
